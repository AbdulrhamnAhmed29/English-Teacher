/**
 * Ordered list of education milestones. Keys map into locales/*.json
 * under `journey.items.<key>`.
 * @type {string[]}
 */
export const EDUCATION_KEYS = ["english_course", "diploma", "accounting",];

/**
 * Ordered list of experience milestones.
 * @type {string[]}
 */
export const EXPERIENCE_KEYS = ["kindergarten"];
